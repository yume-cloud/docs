---
title: "Глоссарий: Автосмена состояния инвентаря"
icon: "book"
description: "Справочник моделей, полей и справочников, участвующих в автоматическом переводе состояния инвентаря при движении ремонтной задачи по воронке."
---

Справочник к странице [«Автосмена состояния инвентаря»](/ru/logic/workshop/inventory-state).

## Понятия

| Понятие | Значение |
| --- | --- |
| Этап воронки | Колонка kanban-воронки, через которую движутся задачи; несёт цвет, порядок и целевое состояние инвентаря. При попадании задачи на этап инвентарь переводится в целевое состояние. |
| Ремонтная задача | Задача мастерской (единица ремонта/сервиса), движущаяся по этапам воронки; агрегирует стоимость, оплаты и прибыль. |
| Автоперевод состояния инвентаря | Сигнал, срабатывающий после сохранения задачи: переводит связанный инвентарь в целевое состояние этапа, если оно задано и отличается от текущего. |

## Модели и поля

### Этап воронки

*Колонка kanban-воронки, через которую движутся задачи; несёт цвет, порядок и целевое состояние инвентаря. При попадании задачи на этап инвентарь переводится в целевое состояние; запись изолируется по арендатору и не хранит меток времени создания/обновления.*

**Идентификация**

| Поле | Тип | Описание |
| --- | --- | --- |
| Название | текст | Название этапа. |
| Статус | перечисление | Статус задачи, соответствующий этапу. |
| Цвет | текст | Цвет колонки в HEX (#ffffff). |

**Порядок**

| Поле | Тип | Описание |
| --- | --- | --- |
| Порядок | число | Позиция этапа в воронке; при вставке остальные сдвигаются на единицу. |

**Влияние на инвентарь**

| Поле | Тип | Описание |
| --- | --- | --- |
| Занимать инвентарь | флаг | Считается ли единица занятой ремонтом, пока открытая задача стоит на этом этапе; по умолчанию включён. Независим от целевого состояния: состояние с флагом «блокирует прокат» выводит единицу из проката само по себе. |

**Связи**

| Поле | Тип | Описание |
| --- | --- | --- |
| Воронка | связь | Воронка, которой принадлежит этап. |
| Целевое состояние инвентаря | связь | Состояние, в которое переводится инвентарь при попадании задачи на этап. |
| Компания | связь | Владелец записи (изоляция по компании). |

**Прочее**

| Поле | Тип | Описание |
| --- | --- | --- |
| Доп. данные | объект | Произвольные данные (JSON). |

### Ремонтная задача

*Задача мастерской (единица ремонта/сервиса), движущаяся по этапам воронки; агрегирует стоимость, оплаты и прибыль. Стоимости пересчитываются автоматически по расходникам, услугам и платежам.*

**Идентификация**

| Поле | Тип | Описание |
| --- | --- | --- |
| Название | текст | Название задачи (обязательно). |
| Номер | текст | Номер задачи. |
| Тип задачи | перечисление | Внутренняя или внешняя. |

**Статус**

| Поле | Тип | Описание |
| --- | --- | --- |
| Статус | перечисление | Текущий статус задачи. |
| Направление операции | перечисление | Тип проводки; по умолчанию расход. |

**Связи**

| Поле | Тип | Описание |
| --- | --- | --- |
| Этап | связь | Этап воронки, на котором находится задача. |
| Автор | связь | Пользователь, создавший задачу (только чтение). |
| Исполнитель | связь | Пользователь-исполнитель; при первой смене статуса подставляется текущий пользователь. |
| Клиент | связь | Клиент, для которого выполняется задача. |
| Заявка на аренду | связь | Связанная аренда. |
| Позиция заявки | связь | Конкретная позиция инвентаря в аренде. |
| Единица инвентаря | связь | Ремонтируемая единица инвентаря; её состояние меняется по этапу. |
| Точка проката | связь | Точка проката (обязательно). |

**Деньги**

| Поле | Тип | Описание |
| --- | --- | --- |
| Стоимость | число (2 знака) | Итоговая стоимость = расходники + услуги (вычисляется, только чтение). |
| Стоимость услуг | число (2 знака) | Сумма по услугам (вычисляется, только чтение). |
| Стоимость расходников | число (2 знака) | Сумма по расходникам (вычисляется, только чтение). |
| Оплачено | число (2 знака) | Сумма платежей с учётом знака: возврат уменьшает значение (вычисляется, только чтение). |

**Сроки**

| Поле | Тип | Описание |
| --- | --- | --- |
| Завершена в | дата/время | Момент завершения; проставляется автоматически при переходе в статус «Завершена». |
| Порядок сортировки | число | Позиция задачи внутри статуса; при вставке остальные сдвигаются на единицу. |
| Создана | дата/время | Момент создания (можно переопределить при создании). |
| Обновлена | дата/время | Момент последнего изменения. |

**Прочее**

| Поле | Тип | Описание |
| --- | --- | --- |
| Доп. данные | объект | Произвольные данные (JSON). |
| Компания | связь | Владелец записи (изоляция по компании). |

### Автоперевод состояния инвентаря

*Сигнал после сохранения задачи: переводит связанный инвентарь в целевое состояние этапа. Срабатывает только если у этапа задано целевое состояние и оно отличается от текущего.*

## Статусы и справочники

### Тип ремонтной задачи

- Внутренняя
- Внешняя

### Состояние инвентаря

Справочник состояний единицы инвентаря; на него ссылается целевое состояние этапа воронки и состояние единицы инвентаря.
