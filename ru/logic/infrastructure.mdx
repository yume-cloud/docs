---
title: "Разделение данных компаний и фоновая обработка"
icon: "server"
description: "Как Yume держит данные каждой компании отдельно от других, что выполняется сразу, а что — в фоне, и как хранятся файлы и журнал событий."
---

Этот раздел объясняет правила, общие для всей платформы: как данные одной компании отделены от данных других, что происходит в момент вашего действия, а что доделывается позже в фоне, и как система хранит файлы и записывает события. Знать это полезно, чтобы понимать, почему некоторые изменения видны сразу, а некоторые — с задержкой.

## Данные компаний разделены

Каждая запись в системе — аренда, клиент, единица инвентаря, оплата — принадлежит одной компании. Все запросы выполняются в контексте той компании, в которой вы работаете, и данные других компаний в них не попадают. Это касается не только списков на экране, но и отчётов, выгрузок, файлов и фоновой обработки.

Если у вашего аккаунта есть доступ сразу к нескольким компаниям, переключение между ними меняет контекст полностью: вы видите только данные выбранной компании. Подробнее о том, как аккаунт связан с компанией и как распределяются права, — в разделе [«Пользователи и доступ»](/ru/logic/user).

<Note>
Файлы и вложения хранятся в облачном хранилище, и каждый файл привязан к своей компании — так же, как записи в базе. Данные GPS и телематики хранятся отдельно от основных, потому что их объём и характер обращения к ним другие.
</Note>

## Что выполняется сразу, а что в фоне

Часть работы система делает немедленно, в момент вашего действия, — например, пересчитывает суммы аренды при изменении её состава. Другая часть выполняется фоновой обработкой: рассылка уведомлений, начисление автоматических штрафов, обновление аналитики, синхронизация с внешними сервисами.

Практическое следствие: результат фонового действия может появиться не мгновенно. Уведомление приходит через несколько секунд после события, а показатели аналитики обновляются по расписанию, а не в момент открытия отчёта. Какие именно процессы работают в фоне и с какой периодичностью — в разделе [«Фоновые процессы»](/ru/logic/background-tasks).

## Журнал событий

Система записывает ход выполнения запросов и ошибки. Записи привязаны к компании и к конкретному обращению, поэтому при обращении в поддержку по конкретной проблеме её можно найти и разобрать. Журнал — внутренний инструмент, через интерфейс он не доступен; при обращении достаточно описать, что и когда вы делали.

## Разделы

<CardGroup cols={2}>
  <Card title="Фоновые процессы" icon="gears" href="/ru/logic/background-tasks">
    Какие процессы выполняются по расписанию, какие по событию, и с какой периодичностью обновляются данные.
  </Card>
  <Card title="Пользователи и доступ" icon="user-lock" href="/ru/logic/user">
    Обзорная страница раздела: аккаунт, участие в компании, роли и права.
  </Card>
</CardGroup>
