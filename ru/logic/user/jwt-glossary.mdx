---
title: "Глоссарий: JWT: выпуск, обновление, кэш"
icon: "book"
description: "Справочник моделей, классов и модулей, участвующих в JWT-аутентификации: remap идентификаторов, кэширование пользователя и обновление токенов."
---

Справочник к странице [«JWT: выпуск, обновление, кэш»](/ru/logic/user/jwt).

## Понятия

| Понятие | Значение |
| --- | --- |
| Артефакт миграции | Немигрируемая таблица соответствия старых и новых идентификаторов записей, перенесённых между компаниями; для JWT фильтруется по таблице-источнику пользователей. |
| Кастомная JWT-аутентификация | DRF-класс аутентификации по умолчанию: извлекает пользователя из access-токена, применяет remap старого идентификатора в новый и кэширует объект пользователя на 5 минут. |
| Схема JWT для документации | Класс drf-spectacular, указывающий кастомную JWT-аутентификацию как целевой класс для генерации OpenAPI-схемы безопасности. |
| Сериализатор обновления токена | Сериализатор эндпоинта обновления: применяет remap идентификатора, проверяет пользователя и ротирует refresh-токен. |
| Кэш аутентификации | Модуль-помощник для формирования ключа кэша пользователя и инвалидации кэша по всем компаниям, где пользователь состоит. |

## Модели и поля

### Артефакт миграции

*Немигрируемая таблица, хранящая соответствие старых и новых идентификаторов записей, перенесённых между компаниями (используется для remap идентификаторов пользователей). Таблица неуправляемая; для JWT фильтруется по таблице-источнику пользователей.*

| Поле | Тип | Описание |
| --- | --- | --- |
| Компания-источник | текст | Идентификатор/название компании-источника записи. |
| Исходная таблица | текст | Имя таблицы-источника; для пользователей — таблица учётных записей. |
| Исходный столбец | текст | Имя столбца-источника идентификатора. |
| Старый идентификатор | число | Прежний идентификатор записи до миграции. |
| Новый идентификатор | число | Текущий идентификатор записи после миграции. |

### Кастомная JWT-аутентификация

*DRF-класс аутентификации по умолчанию: извлекает пользователя из access-токена, применяет remap старого идентификатора в новый и кэширует объект пользователя на 5 минут. Ключ кэша обособлен по компании (namespaced).*

| Поле | Тип | Описание |
| --- | --- | --- |
| Получение пользователя по токену | метод | Читает идентификатор пользователя из клейма токена, при установленном контексте компании (RLS) применяет remap через артефакт миграции, берёт пользователя из кэша или базы данных, проверяет активность и кэширует. |
| Флаг remap артефактов | флаг (константа модуля) | Включает поиск remap старого идентификатора в новый, когда на соединении установлен контекст компании. |

### Схема JWT для документации

*Класс drf-spectacular, указывающий кастомную JWT-аутентификацию в качестве целевого класса для генерации OpenAPI-схемы безопасности.*

| Поле | Тип | Описание |
| --- | --- | --- |
| Целевой класс аутентификации | текст (путь к классу) | Указывает на кастомную JWT-аутентификацию. |

### Сериализатор обновления токена

*Сериализатор для эндпоинта обновления: применяет remap старого идентификатора в новый, проверяет пользователя и ротирует refresh-токен. Ветка обработки отсутствующего пользователя недостижима — при отсутствии пользователя валидация не срабатывает и access-токен всё равно выдаётся.*

| Поле | Тип | Описание |
| --- | --- | --- |
| Валидация и обновление | метод | Разбирает refresh-токен, при установленном контексте компании (RLS) применяет remap и обновляет идентификатор пользователя в payload, проверяет правило аутентификации пользователя, ротирует refresh-токен при включённой ротации. |

### Кэш аутентификации

*Модуль-помощник для формирования ключа кэша пользователя и инвалидации кэша по всем компаниям, где пользователь состоит. Время жизни кэша — 5 минут.*

| Поле | Тип | Описание |
| --- | --- | --- |
| Время жизни кэша пользователя | число (секунды) | 5 минут (300 секунд). |
| Ключ кэша пользователя | функция | Возвращает ключ по идентификатору пользователя; кэш обособлен по компании (namespaced) автоматически. |
| Компании пользователя | функция | Собирает набор компаний, где пользователь является администратором, плюс глобальный кэш (без привязки к компании), где также закэширован пользователь. |
| Инвалидация кэша пользователя | функция | Удаляет ключ пользователя по всем его компаниям, с временной установкой RLS-контекста каждой компании. |
